using UnityEngine;
using System.Collections;

public class spawnParticle : MonoBehaviour {
	/*
	public int amount;
	public GameObject toSpawn;
	public myvector handler;

	// Use this for initialization
	void Start () {
		for (var i = 0; i < amount; i++) {
			 instantiates the object "tospawn" (particle) in the middle of the screen and not rotated
			 * instantiate gives an object, and list.add takes a gameobject, so you have to add the "as gameobject" to make instantiate return a gameobject
			 * havent tested under 1 amount
			
			GameObject spawnedObj = Instantiate (toSpawn, new Vector3 (i-amount/2, 0, 0), new Quaternion ()) as GameObject;
			// we now have the gameobject and can access its components and stuff
			spawnedObj.GetComponent<ParticleCharge>().charge = Random.Range(-1,2);
			handler.particles.Add(spawnedObj);
		}
		Debug.Log (amount + " particles spawned");
		Debug.Log (handler.particles.Count + " particles in array");
	}
	
	// Update is called once per frame
	void Update () {
	
	}*/
}
